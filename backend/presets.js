module.exports = {
  portrait_soft: {
    scale: 1.4,
    median: 1,
    sharpen: {
      sigma: 0.9,
      m1: 0.6,
      m2: 1.4,
      x1: 2,
      y2: 12,
    },
    modulate: {
      brightness: 1.01,
      saturation: 1.03,
    },
  },

  portrait_clear: {
    scale: 1.6,
    median: 1,
    sharpen: {
      sigma: 1.2,
      m1: 1.0,
      m2: 2.0,
      x1: 2,
      y2: 15,
    },
    modulate: {
      brightness: 1.02,
      saturation: 1.05,
    },
  },

  portrait_strong: {
    // 图像放大倍率（相对于原始宽度）
    // 1.7 表示将图片放大到原来的 170%
    // 用于提升截图人像的“观感清晰度”
    // ⚠️ 放大越多，对锐化和去噪的要求越高
    scale: 1.7,

    // 中值滤波半径（去噪）
    // 1 = 非常轻微去噪
    // 作用：
    // - 抑制截图压缩噪点
    // - 防止锐化时噪点被一同放大
    // ⚠️ 数值过大会让皮肤变“糊”
    median: 1,

    sharpen: {
      // 高斯模糊半径（决定“锐化作用范围”）
      // 数值越大，锐化影响的区域越宽
      // 1.35 属于：偏强但仍适合人像
      // 主要作用在轮廓、五官边缘
      sigma: 1.35,

      // 第一层锐化强度（边缘增强强度）
      // 控制轮廓的清晰程度（下巴、眼睛、鼻梁）
      // 1.2 = 明显增强，但不至于过硬
      m1: 1.2,

      // 第二层锐化强度（细节增强）
      // 控制发丝、睫毛、眉毛等细节
      // 数值偏高会让皮肤显粗糙
      // 2.4 用于“强清晰风格”
      m2: 2.4,

      // 边缘检测阈值（低阈值）
      // 小于该值的变化会被认为是“噪点”
      // 2 = 忽略极小变化，减少噪点被锐化
      x1: 2,

      // 边缘检测阈值（高阈值）
      // 大于该值的变化被认为是“明确边缘”
      // 18 = 明确强调轮廓线条
      y2: 18,
    },

    modulate: {
      // 亮度调节
      // 1.03 = 轻微提亮
      // 目的是抵消锐化后画面偏“硬暗”的感觉
      brightness: 1.03,

      // 饱和度调节
      // 1.08 = 明显但安全的增强
      // 用于恢复截图中被压缩的肤色和唇色
      // ⚠️ 数值过高会显得“假”
      saturation: 1.08,
    },
  },

  screenshot_ui: {
    scale: 1.6,
    median: 0,
    sharpen: {
      sigma: 1.4,
      m1: 1.3,
      m2: 2.5,
      x1: 2,
      y2: 20,
    },
    modulate: {
      brightness: 1.0,
      saturation: 1.0,
    },
  },
};
